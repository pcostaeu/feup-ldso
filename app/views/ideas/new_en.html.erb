<div class="row" id="submitIdea">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center">Got a
        <strong>idea?</strong>
      </h2>
      <hr>
      <p>We need your help to make FEUP better. If you have a idea to make it more substainable please send us your idea. The best ideas will earn a prize</p>
      <%= form_for :idea, url: ideas_path do |f| %>
      <div class="row">
        <div class="form-group" id="participantesForm">
     <div class="form-group col-lg-6">
       <%= f.label :author, "Nome" %>
       <%= f.text_field :author, :class => 'form-control',  :required => true  %>
     </div>
     <div class="form-group col-lg-6">
       <%= f.label :email, "Email" %>
       <%= f.text_field :email, :class => 'form-control' , :required => true, :pattern => '[^@]+@[^@]+\.[a-zA-Z]{2,6}' %>
     </div>
       </div>
              <div class="form-group col-lg-6">
        <button type="button" id="participante" class="btn btn-default btn-md">
            <span class="icon-stack">
                <i class="glyphicon glyphicon-plus"></i>
                <i class="glyphicon glyphicon-user"></i>
        Add participant
          </button>
         </div>
             <div class="form-group col-lg-6">
        <button type="button" id="participanteRem" class="btn btn-default btn-md" >
            <span class="icon-stack">
                <i class="glyphicon glyphicon-minus"></i>
                <i class="glyphicon glyphicon-user"></i>
        Remove Participant
          </button>
         </div>
        <div class="clearfix"></div>
        <div class="form-group col-lg-12">
          <%= f.label :title, "Title" %>
          <%= f.text_field :title, :class => 'form-control', :rows => '6', :required => true %>
        </div>
                 <div class="clearfix"></div>
        <div class="form-group col-lg-12">
            <%= f.label :text, "Resume" %>
            <%= f.text_area :resumo, :style => 'resize: vertical;', :type => 'text', :maxlength => 350, :class => 'form-control', :rows => '6', :required => true %>
        </div>
        <div class="clearfix"></div>
        <div class="form-group col-lg-12">
            <%= f.label :text, "Idea description" %>
          <%= f.text_area :text, :style => 'resize: vertical;', :type => 'text', :maxlength => 3000, :class => 'form-control', :rows => '6' %>
        </div>
        <div class="clearfix"></div>
        <div class="form-group col-lg-12">
          <label style="float: left">Select a image:</label>
          <div class="col-lg-6">
            <%= f.file_field :picture %>
          </div>
        </div>
           <div class="form-group col-lg-12">
                <h5>Terms and conditions</h5>
                <ul>
              <li>Your idea can be released anywhere.</li>
            </ul>
        </div>
        <% if @contact.try(:errors).try(:any?) %>
        <% @contact.errors.each do |attribute, error| %>
        <ul>
          <li><small class="text-danger"> <%= error %> </small></li>
        </ul>
      <% end %>
        <% end %>
        <div class="form-group col-lg-12">
            <%= f.submit "Submit", :class => 'btn btn-default', data: { confirm: 'Please confirm all data provided. It can`t be modified after submition. Are you sure?' } %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
