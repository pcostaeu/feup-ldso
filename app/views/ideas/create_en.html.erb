<div class="row" id="submitIdea">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center">Got an
        <strong>idea?</strong>
      </h2>
      <hr>
      <p>We need your help to make FEUP better. If you have a idea to make it more substainable please send us your idea. The best ideas will earn a prize.</p>
      <%= form_for :idea, url: ideas_path do |f| %>
      <div class="row">
        <div class="form-group" id="participantesForm">
          <div class="form-group col-lg-6">
            <%= f.label :author, "Name" %>
            <%= f.text_field :author, :class => 'form-control',  :required => true, :'data-errormessage-value-missing' => 'Nome obrigatório'  %>
          </div>
          <div class="form-group col-lg-6">
            <%= f.label :email, "Email" %>
            <%= f.text_field :email, :class => 'form-control' , :required => true, :'data-errormessage-value-missing' => 'Email obrigatório', :pattern => '([A-Za-z0-9]*)@fe.up.pt' %>
          </div>
        </div>
        <div class="form-group col-lg-6">
          <button class="btn btn-default btn-md" id="participante" type="button">
            <span class="icon-stack">
              <i class="glyphicon glyphicon-plus"></i>
              <i class="glyphicon glyphicon-user"></i>
              Add Participant
            </button>
          </div>
          <div class="form-group col-lg-6">
            <button class="btn btn-default btn-md" id="participanteRem" type="button">
              <span class="icon-stack">
                <i class="glyphicon glyphicon-minus"></i>
                <i class="glyphicon glyphicon-user"></i>
                Remove Participant
              </button>
            </div>
            <div class="clearfix"></div>
            <div class="form-group col-lg-12">
              <%= f.label :title, "Title" %>
              <%= f.text_field :title, :class => 'form-control', :required => true, :'data-errormessage-value-missing' => 'Title is mandatory' %>
            </div>
            <div class="clearfix"></div>
            <div class="form-group col-lg-12">
              <%= f.label :resumo, "Resume" %>
              <%= f.text_area :resumo, :style => 'resize: vertical;', :type => 'text', :maxlength => 350, :class => 'form-control', :rows => '3', :required => true, :'data-errormessage-value-missing' => 'Resume is mandatory' %>
            </div>
            <div class="clearfix"></div>
            <div class="form-group col-lg-12">
              <%= f.label :text, "Idea description" %>
              <%= f.text_area :text, :style => 'resize: vertical;', :type => 'text', :maxlength => 3000, :class => 'form-control', :rows => '6' %>
            </div>
            <div class="clearfix"></div>
            <div class="form-group col-lg-12">
              <label style="float:left; padding-right: 5px">Choose an image that identifies the problem:</label>
              <%= f.file_field :picture %>
            </div>
            <div class="form-group col-lg-12">
              <h5>Termos e condições</h5>
              <ul class="auts-list">
                <li><%= f.radio_button :auts, 'aut_full', :checked => true %>
                  Authorize the disclosure of the idea without limitations.</li>
                <li><%= f.radio_button :auts, 'aut_parcial' %>
                  Authorize the release of the title, abstract of the idea.</li>
              </ul>
            </div>
            <div class="form-group col-lg-12">
              <%= f.submit "Accept and Submit", :class => 'btn btn-default', data: { confirm: 'Confirm the entered data. After submission it\'s not possible to make changes. Do you want to submit?' } %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
