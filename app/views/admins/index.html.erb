<%= render "layouts/admin_header" %>

<div class="container" id="ideas">
  <div class="row">
    <div class="box">
      <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center">Gestão de
          <strong>administradores</strong>
        </h2>
        <hr>
        <div class="col-lg-6">
          <h4 class="intro-text text-center">Contas existentes</h4>
          <ul id="listAdmins">
            <% Admin.all.each do |admin| %>
            <li class="push-down">
              <strong><%= admin.email %></strong>
            </li>
            <%= button_to 'X', admin, method: :delete, role:"button", class:"btn btn-danger btn-xs push-right", data: { confirm: 'Tem a certeza?' } %>
            <br>
            <% end %>
          </ul>
        </div>
        <div class="col-lg-6">
          <h4 class="intro-text text-center">Criar conta</h4>
          <%= render 'form' %>
        </div>
        <div class="col-lg-12"><hr></div>
      </div>
      <div class="col-lg-12">
        <h4 class="intro-text text-center">Editar password da conta</h4>
        <div class="col-lg-3"></div>
        <div class="col-lg-6">
          <%= render 'edit' %>
        </div>
      </div>
    </div>
  </div>
</div>
